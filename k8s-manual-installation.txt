rsa keygen commands:

ssh-keygen -t rsa -b 4096 -f ~/.ssh/prudhviraju_rsa -C "prudhviraju@LAPTOP"
ls ~/.ssh-keygen
cat ~/.ssh/prudhviraju_rsa.pub

Pre-requisites:

STEP 1: Create VPC Firewall Rules (ONCE)
1️⃣ Go to:
VPC Network → Firewall
2️⃣ Create internal Kubernetes rule

Name: k8s-internal

Direction: Ingress

Target: All instances in network

Source IP ranges: 10.0.0.0/8

Protocols & ports:

tcp:6443
tcp:2379-2380
tcp:10250
tcp:30000-32767
udp:8472

Click Create

STEP 2: Configure Cloud NAT

This is the right way and what you should learn.

We will:

Keep k8s-master private

Allow outbound-only internet access

No inbound exposure

 STEP 1: Check Your Network & Subnet

GCP Console → VPC network → VPC networks

Click your VPC (likely default)

Note the subnet name and region

Example:

Subnet: default
Region: asia-south1
 STEP 2: Create Cloud Router

Go to:

Network services → Cloud Router

Click Create Router

Fill:

Field	Value
Name	k8s-router
Network	default
Region	SAME region as VMs

Click Create

STEP 3: Create Cloud NAT

Go to:

Network services → Cloud NAT

Click Create NAT gateway

Fill:

Field	Value
Name	k8s-nat
Cloud Router	k8s-router
NAT type	Public NAT
Subnet	All subnets (or your subnet)
Source IP ranges	All IP ranges
NAT IP addresses	Auto-allocate

Click Create

⏳ Wait 1–2 minutes.


k8s Installation:

1️. Disable swap (Kubernetes requirement)

sudo swapoff -a
sudo sed -i '/ swap / s/^/#/' /etc/fstab

2. Load required kernel modules

cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

sudo modprobe overlay
sudo modprobe br_netfilter

3. Set required sysctl params

cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward = 1
EOF

sudo sysctl --system

4. INSTALL containerd

sudo apt update
sudo apt install -y containerd
sudo mkdir -p /etc/containerd
containerd config default | sudo tee /etc/containerd/config.toml
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' \
/etc/containerd/config.toml
sudo systemctl restart containerd
sudo systemctl enable containerd

5. INSTALL Kubernetes Binaries

sudo apt update
sudo apt install -y apt-transport-https ca-certificates curl

curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key \
| sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes.gpg

echo "deb [signed-by=/etc/apt/keyrings/kubernetes.gpg] \
https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /" \
| sudo tee /etc/apt/sources.list.d/kubernetes.list

sudo apt update
sudo apt install -y kubeadm kubelet kubectl
sudo apt-mark hold kubeadm kubelet kubectl

6. INIT CLUSTER (MASTER ONLY)

sudo kubeadm init --pod-network-cidr=192.168.0.0/16

# save the Join command and add sudo as prefix

7. Configure kubectl(master only)

mkdir -p ~/.kube
sudo cp /etc/kubernetes/admin.conf ~/.kube/config
sudo chown $(id -u):$(id -g) ~/.kube/config

8. INSTALL CNI (MASTER)

kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.27.0/manifests/calico.yaml

9. JOIN WORKERS

#Run saved join command on:

#k8s-worker1

#k8s-worker2

10. verify

kubectl get nodes -o wide

sudo kubeadm join 10.128.0.14:6443 --token wu9688.n7mr6srdujcoopq9 \
        --discovery-token-ca-cert-hash sha256:a478cc4614f8f9a2440e2413ad5e10a9656884bef9b9ab8bdf7f57d9995db929

